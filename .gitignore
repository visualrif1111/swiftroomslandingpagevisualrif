node_modules
.vercel

# Build output — generated by `vite build`; Vercel builds from source
dist

# Environment / secrets — never commit real values (keep .env.example only)
.env
.env.*
!.env.example

# Logs & OS cruft
*.log
npm-debug.log*
.DS_Store
